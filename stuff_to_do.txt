1. Add game state to `Board` first.
- `side_to_move`, `castling_rights`, `en_passant_target`, `halfmove_clock`, `fullmove_number`.
- Extend FEN parse/serialize to include all 6 FEN fields.

2. Separate pseudo-legal vs legal moves.
- Keep current `get_valid_moves()` as pseudo-legal.
- Add board-level `get_legal_moves(piece)` that simulates each move and rejects any leaving own king in check.

3. Implement attack/check detection core.
- `is_square_attacked(x, y, by_color)`
- `is_in_check(color)`

4. Implement turn enforcement.
- Only allow moving pieces of `side_to_move`.
- Toggle turn after successful legal move.
- Increment `fullmove_number` after Black moves.

5. Implement castling.
- Validate king/rook unmoved, clear path, king not in check, transit squares not attacked.
- Move rook automatically.
- Update castling rights when king/rook moves or rook is captured.

6. Implement en passant.
- Set `en_passant_target` after 2-square pawn push.
- Allow only immediate next move capture.
- Remove captured pawn correctly.

7. Implement promotion.
- Trigger when pawn reaches back rank.
- Support choice `Q/R/B/N` (default to queen if no choice provided).

8. Implement end-state detection.
- `checkmate`: in check and no legal moves.
- `stalemate`: not in check and no legal moves.

9. Implement draw rules.
- 50-move rule from `halfmove_clock`.
- Threefold repetition using normalized position key (board + side + castling + en passant).
- Insufficient material detection.

10. Add undo/state history.
- Store reversible move records (moved piece, captured piece, rights, clocks, en passant, promotion/castle flags).
- Use this for move simulation and repetition tracking.

11. Add a test suite before/while building.
- Per-piece movement tests.
- Legal-move filtering tests (pins/check).
- Special rules tests (castle/en passant/promotion).
- End-state and draw-rule tests.

If you want, I can start implementing this in that order and stop after each milestone for review.